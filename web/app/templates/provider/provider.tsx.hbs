type Props = {};

export const {{pascalCase name}}Context = createContext();

export const {{pascalCase name}}Provider = (props: Props) => {
  {{pascalCase name}}Context.of(/* State */);

  return <></>;
};

export default {{pascalCase name}}Provider;
